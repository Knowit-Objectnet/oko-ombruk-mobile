import 'package:test/test.dart';
// import 'package:ombruk/businessLogic/Partner.dart';

void main() {
  // TODO: Unit test Partner
  test('Test correct JSON parsing', () {
    expect(1, 1);
    // expect(Partner.fromJson(jsonFromApi).toJson(), parsedJson);
  });
}
