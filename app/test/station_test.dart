import 'package:test/test.dart';
// import 'package:ombruk/businessLogic/Station.dart';

void main() {
  // TODO: Unit test Station
  test('Test correct JSON parsing', () {
    expect(1, 1);
    // expect(Station.fromJson(jsonFromApi).toJson(), parsedJson);
  });
}
